#!/usr/bin/env perl
use 5.14.0;
use warnings;
use utf8::all;
use Anki::Corpus;

my $limit = 10 || shift;

my $corpus = Anki::Corpus->new(file => $ENV{ANKI_CORPUS});
$corpus->print_each("
    ORDER BY created DESC
    LIMIT $limit
");
